# Copy to .env.local (gitignored) and fill in.
# This is the Managed-Agents-enabled workspace key (the swarm's coordinator +
# specialists live on this workspace). run_swarm.py loads .env.local automatically
# and maps ANTHROPIC_API_KEY_NEW -> ANTHROPIC_API_KEY.
ANTHROPIC_API_KEY_NEW=sk-ant-...
